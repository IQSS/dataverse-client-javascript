export const createFilesTotalDownloadSizePayload = (): { storageSize: number; message: string } => {
  return {
    storageSize: 173,
    message: 'Total size of the files available for download in this version of the dataset: 173 bytes',
  };
};
